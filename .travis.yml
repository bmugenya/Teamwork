sudo: false


language: node_js

node_js:
  - "stable"
 
install:
  - npm ci

services:
  - postgresql
  
before_script:
  - psql -c 'create database Teamwork;' -U postgres
 
 script: queries.sql
  

cache:
  directories:
    - "backend/node_modules"
